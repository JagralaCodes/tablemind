import 'package:flutter/material.dart';

class PreviewDetailPage extends StatelessWidget {
  const PreviewDetailPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
